<Page x:Class="TRX_Launcher.TR1X"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      d:DesignHeight="450"
      d:DesignWidth="800"
      Title="TR1X">

    <Grid Margin="5,33,5,-23">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="53*"/>
            <ColumnDefinition Width="26*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Text="Launch TR1X"
                   FontSize="22"
                   FontWeight="SemiBold"
                   Margin="27,0,0,24" Grid.ColumnSpan="2"/>

        <!-- Radio Buttons -->
        <StackPanel Margin="0,53,0,24" Grid.ColumnSpan="2" Grid.RowSpan="2">
            <RadioButton x:Name="NormalRadio"
                         Content="Normal Game"
                         GroupName="GameMode"
                         IsChecked="True"
                         Margin="27,0,0,8"/>
            <RadioButton x:Name="GoldRadio"
                         Content="Unfinished Business"
                         GroupName="GameMode" RenderTransformOrigin="0.516,0.55" Margin="27,0,0,8"/>
        </StackPanel>

        <!-- Start Button -->
        <Button x:Name="StartButton"
                Grid.Row="1"
                Content="Start"
                Width="100"
                Height="32"
                HorizontalAlignment="Left"
                Click="StartButton_Click" Margin="27,62,0,0" Grid.RowSpan="2"/>
        <Image HorizontalAlignment="Left" Height="218" Margin="395,20,0,0" VerticalAlignment="Top" Width="219" RenderTransformOrigin="-0.838,1.002" Source="IMG\TR1X.png" Grid.ColumnSpan="2" Grid.RowSpan="4"/>
    </Grid>
</Page>
